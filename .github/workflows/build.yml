name: Test Workflow

on:
  push:
    branches:
      - main  # Modifie "main" par le nom de ta branche si nécessaire

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Say Hello
      run: echo "Hello, GitHub Actions is working!"
