name: Build and Release Tauri App

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Étape 1 : Cloner le dépôt
    - name: Checkout Repository
      uses: actions/checkout@v3

    # Étape 2 : Installer les dépendances système nécessaires
    - name: Install System Dependencies
      run: |
        sudo apt-get update
        # Installer GTK et ses dépendances (y compris GDK)
        sudo apt-get install -y \
          pkg-config \
          libglib2.0-dev \
          libgtk-3-dev \
          libgdk-pixbuf2.0-dev

    # Étape 3 : Installer Rust
    - name: Setup Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true

    # Étape 4 : Installer Node.js
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    # Étape 5 : Installer les dépendances du projet
    - name: Install Dependencies
      run: npm install

    # Étape 6 : Construire l'application Tauri
    - name: Build Tauri App
      run: npm run tauri build

  release:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && contains(github.ref, 'main')

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Upload Release Asset
      uses: actions/upload-artifact@v3
      with:
        name: Tauri Build
        path: src-tauri/target/release/bundle/
